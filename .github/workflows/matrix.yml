name: Matrix
on: push

jobs:
  node-version:
    runs-on: ubuntu-latest
    steps:
      - name: Pull down node
        uses: actions/setup-node@v2
          with:
            node-version: '14'
      - name: Log node version
        run: node -v